# docker-compose.yml
version: "3.8"
services:
  server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    # override the board or port here if you want
    command: ["8080", "boards/perfect.txt"]
    restart: unless-stopped
